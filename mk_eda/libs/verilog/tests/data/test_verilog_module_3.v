module Top(a0, a2, a1, sel, out_0, out_1);
    input a0;
    input a2;
    input a1;
    input sel;
    output out_0;
    output out_1;
    wire a0;
    wire a1;
    wire a2;
    wire sel;
    wire out_0;
    wire out_1;
    wire z0; wire z1; wire z2; wire z3;
    wire w1, w2, w3, w4, w5, w6, w7;
    not (z0, a0);
    xnor (z1, a0, a1);
    or (w1, a0, a1);
    xnor (z2, a2, w1);
    and (w2, a2, a1);
    and (w3, a2, a0);
    nor (z3, w2, w3);
    nor (w4, sel, z3);
    nor (w5, sel, z2);
    and (w6, sel, z1);
    and (w7, sel, z0);
    or (out_1, w4, w6);
    or (out_0, w5, w7);
endmodule
